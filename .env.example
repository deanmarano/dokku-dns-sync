# AWS Credentials for DNS Plugin Testing
# Copy this to .env and fill in your AWS credentials
# The .env file is git-ignored for security

# Required for AWS Route53 access
AWS_ACCESS_KEY_ID=your_access_key_id_here
AWS_SECRET_ACCESS_KEY=your_secret_access_key_here

# Optional - defaults to us-east-1 if not specified
AWS_DEFAULT_REGION=us-east-1

# Test server configuration (optional - can also use command line args)
#SSH_SERVER=your-server.com
#REMOTE_USER=your-ssh-user