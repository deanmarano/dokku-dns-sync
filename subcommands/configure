#!/usr/bin/env bash
source "$(dirname "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)")/config"
set -eo pipefail
[[ $DOKKU_TRACE ]] && set -x
source "$PLUGIN_CORE_AVAILABLE_PATH/common/functions"
source "$(dirname "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)")/functions"

service-configure-cmd() {
  #E configure the global DNS sync backend
  #E dokku $PLUGIN_COMMAND_PREFIX:configure [aws|cloudflare]
  #E this sets up the DNS provider backend for all DNS synchronization.
  #E if no backend is specified, defaults to '$DNS_SYNC_DEFAULT_BACKEND'
  #E after configuration, use other commands to:
  #E - configure credentials: dokku $PLUGIN_COMMAND_PREFIX:backend-auth
  #E - sync an app: dokku $PLUGIN_COMMAND_PREFIX:sync myapp
  #A backend, DNS provider backend (aws, cloudflare) - optional, defaults to aws
  declare desc="configure the global $PLUGIN_SERVICE backend"
  local cmd="$PLUGIN_COMMAND_PREFIX:configure" argv=("$@")
  [[ ${argv[0]} == "$cmd" ]] && shift 1
  declare BACKEND="$1"

  # Use default backend if not specified
  if [[ -z "$BACKEND" ]]; then
    BACKEND="$DNS_SYNC_DEFAULT_BACKEND"
  fi
  
  # Validate backend
  case "$BACKEND" in
    aws|cloudflare)
      ;;
    *)
      dokku_log_fail "Invalid backend '$BACKEND'. Supported backends: aws, cloudflare"
      ;;
  esac
  
  dns_configure_global "$BACKEND"
}

service-configure-cmd "$@"
