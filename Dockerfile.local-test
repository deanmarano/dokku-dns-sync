FROM ubuntu:22.04

# Avoid interactive prompts during package installation
ENV DEBIAN_FRONTEND=noninteractive

# Install basic dependencies
RUN apt-get update && apt-get install -y \
    curl \
    git \
    sudo \
    wget \
    bash \
    openssh-client \
    awscli \
    dnsutils \
    netcat-openbsd \
    && rm -rf /var/lib/apt/lists/*

# Install BATS (Bash Automated Testing System)
RUN git clone https://github.com/bats-core/bats-core.git /tmp/bats-core \
    && cd /tmp/bats-core \
    && ./install.sh /usr/local \
    && rm -rf /tmp/bats-core

# Install shellcheck for linting
RUN apt-get update && apt-get install -y shellcheck && rm -rf /var/lib/apt/lists/*

# Set up working directory
WORKDIR /plugin

# Copy wait and test script
COPY wait-and-test.sh /wait-and-test.sh
COPY test-docker.sh /test-docker.sh
RUN chmod +x /wait-and-test.sh /test-docker.sh

# Default command
CMD ["/wait-and-test.sh"]